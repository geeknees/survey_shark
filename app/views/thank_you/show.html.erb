<div class="min-h-screen bg-gray-50 flex items-center justify-center py-12 px-4 sm:px-6 lg:px-8">
  <div class="max-w-md w-full space-y-8">
    <div class="text-center">
      <!-- Success Icon -->
      <div class="mx-auto flex items-center justify-center h-16 w-16 rounded-full bg-green-100 mb-6">
        <svg class="h-8 w-8 text-green-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
        </svg>
      </div>
      
      <!-- Thank You Message -->
      <h1 class="text-2xl font-bold text-gray-900 mb-4">
        ご協力ありがとうございました
      </h1>
      
      <p class="text-gray-600 mb-8">
        回答は無事に送信されました。<br>
        貴重なご意見をお聞かせいただき、ありがとうございます。
      </p>
      
      <!-- Action Buttons -->
      <div class="space-y-4">
        <% if @project.active? && !ProjectKpis.new(@project).is_at_limit? %>
          <%= form_with url: restart_project_thank_you_path(@project), method: :post, local: true do |form| %>
            <%= form.submit "もう一度回答する", 
                class: "w-full bg-blue-600 hover:bg-blue-700 text-white font-medium py-3 px-4 rounded-lg transition-colors" %>
          <% end %>
          
          <p class="text-xs text-gray-500">
            新しい回答として記録されます
          </p>
        <% else %>
          <div class="bg-gray-100 text-gray-600 font-medium py-3 px-4 rounded-lg">
            募集は終了しました
          </div>
        <% end %>
      </div>
      
      <!-- Project Info -->
      <div class="mt-8 pt-6 border-t border-gray-200">
        <p class="text-sm text-gray-500">
          プロジェクト: <%= @project.name %>
        </p>
        <% kpis = ProjectKpis.new(@project) %>
        <p class="text-xs text-gray-400 mt-1">
          回答数: <%= kpis.total_responses %>/<%= kpis.max_responses %>
        </p>
      </div>
    </div>
  </div>
</div>